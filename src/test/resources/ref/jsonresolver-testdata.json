{
    "loops": [
        {
            "id": "a://b.c#",
            "$ref": "a://b.c#"
        },
        {
            "id": "schema://schema2#",
            "$ref": "schema://schema2#"
        }
    ],
    "resolve": [
        {
            "schema": {
                "$ref": "schema://schema1#"
            },
            "expected": "bar"
        },
        {
            "schema": {
                "$ref": "schema://schema4#"
            },
            "expected": 31
        }
    ]
}